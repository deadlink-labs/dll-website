# dependencies
node_modules/

# build output
dist/

# astro
.astro/

# vercel
.vercel/

# content/ committed directly for now (Obsidian pipeline comes later).
# The vault's own config is not site source and never belongs to this repo.
content/.obsidian/

# environment variables
.env
.env.*


# editor / os
.DS_Store

# logs
npm-debug.log*

# local-only working material, never committed and not part of the deliverable.
# Currently .local/voice/ — the two guides defining how Marcelo writes and speaks.
# That is his own voice, not shared tooling; CLAUDE.md §6 keeps the public summary
# and says so. Anything else machine-local belongs here too.
.local/